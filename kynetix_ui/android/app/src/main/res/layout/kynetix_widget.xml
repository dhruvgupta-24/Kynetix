<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/kynetix_widget_background"
    android:padding="12dp">

    <!-- Header Section (visible on 4x4 and large sizes) -->
    <LinearLayout
        android:id="@+id/widget_header_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_alignParentTop="true"
        android:layout_marginBottom="8dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="KYNETIX"
            android:textColor="#52B788"
            android:textSize="12sp"
            android:textStyle="bold" />

        <FrameLayout
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <TextView
            android:id="@+id/widget_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Today"
            android:textColor="#9CA3AF"
            android:textSize="11sp" />
    </LinearLayout>

    <!-- Main Content Container: stacks/aligns rings and detail text -->
    <LinearLayout
        android:id="@+id/widget_content_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/widget_header_layout"
        android:orientation="horizontal"
        android:gravity="center">

        <!-- Rings container: ImageView + center text overlay -->
        <FrameLayout
            android:id="@+id/widget_rings_container"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_gravity="center">

            <ImageView
                android:id="@+id/widget_progress_rings"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="fitCenter" />

            <!-- Center overlay text -->
            <LinearLayout
                android:id="@+id/widget_center_text_layout"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:gravity="center"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/widget_center_value"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="--"
                    android:textColor="#FFFFFF"
                    android:textSize="18sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/widget_center_label"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="kcal left"
                    android:textColor="#9CA3AF"
                    android:textSize="8sp" />
            </LinearLayout>
        </FrameLayout>

        <!-- Detailed text section (hidden in 2x2, shown in 4x2 / 4x4) -->
        <LinearLayout
            android:id="@+id/widget_details_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginStart="14dp"
            android:orientation="vertical"
            android:visibility="gone">

            <!-- Calories detail row -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:layout_marginBottom="8dp">

                <TextView
                    android:id="@+id/widget_calories_remaining"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="-- kcal left"
                    android:textColor="#FFFFFF"
                    android:textSize="14sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/widget_calories_consumed_target"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="-- / -- kcal"
                    android:textColor="#9CA3AF"
                    android:textSize="11sp" />
            </LinearLayout>

            <!-- Protein detail row -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/widget_protein_remaining"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="--g protein left"
                    android:textColor="#FFFFFF"
                    android:textSize="14sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/widget_protein_consumed_target"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="-- / -- g protein"
                    android:textColor="#9CA3AF"
                    android:textSize="11sp" />
            </LinearLayout>
        </LinearLayout>
    </LinearLayout>

    <!-- Error/Fallback message overlay -->
    <TextView
        android:id="@+id/widget_fallback_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="Open Kynetix to set targets"
        android:textColor="#9CA3AF"
        android:textSize="12sp"
        android:gravity="center"
        android:textStyle="italic"
        android:visibility="gone" />
</RelativeLayout>
